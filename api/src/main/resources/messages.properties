
# INTERVA FORM
${project.parent.artifactId}.confirmProcessRun = Confirm Run
${project.parent.artifactId}.generateInputCsvHeading = IRIS Generate Input CSV
${project.parent.artifactId}.downloadInputCsvText = Download IRIS Input CSV
${project.parent.artifactId}.inputCsvRunConfirm = Click on `Confirm Run` to confirm running the process for IRIS input CSV generation. <br>Note that this <b>process is irreversible</b>. <br>File would be generated at {file location} with name like {file name}
${project.parent.artifactId}.inputCsvRunFilenameSettingMissingError = [**Setting input.fileDirectory not found. Input CSV generation process won`t work**]
${project.parent.artifactId}.inputCsvRunFilelocationSettingMissingError = [**Setting input.filenameStartPattern not found. Input CSV generation process won`t work**]
${project.parent.artifactId}.generateOutputCsvHeading = IRIS Generate Output CSV
${project.parent.artifactId}.outputCsvRunConfirm = Click on `Confirm Run` to confirm starting the process for parsing IRIS output CSV. <br>Note that this <b>process is irreversible</b>. <br>File should exist at {file location} with name {file name}. <br>After parsing CSV successfully, file would be archived in directory {archive location}
${project.parent.artifactId}.outputCsvRunFilenameSettingMissingError = [**Setting output.fileDirectory not found. Output CSV Parser process won`t work**]
${project.parent.artifactId}.outputCsvRunFilelocationSettingMissingError = [**Setting output.filename not found. Output CSV Parser process won`t work**]
